# Simple docker-compose.yml
# You can change your port or volume location

version: '3.3'

services:
  uptime-kuma:
    # image: louislam/uptime-kuma:20240528-14-alpine
    image: uptime-kuma:20250421-17-alpine
    container_name: uptime-kuma
    #volumes:
    #  - ./uptime-kuma-data:/app/data
    environment:
      - UPTIME_TITLE=Hello
      - UPTIME_LOCATION=World
    ports:
      - 3001:3001  # <Host Port>:<Container Port>
    restart: always
